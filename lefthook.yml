pre-commit:
  parallel: true
  jobs:
    - name: lint
      run: pnpm lint:fix -- {staged_files}
      stage_fixed: true

    - name: format
      glob: '*.{ts,tsx,js,jsx,json,css}'
      run: pnpm fmt:fix -- {staged_files}
      stage_fixed: true

    - name: typecheck
      run: pnpm typecheck
