import { ComparePageLayout } from '@/components/compare/compare-page-layout';

export default function ComparePerDollarLayout({ children }: { children: React.ReactNode }) {
  return <ComparePageLayout>{children}</ComparePageLayout>;
}
